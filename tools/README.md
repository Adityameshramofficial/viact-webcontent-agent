# Tools

Python scripts for deterministic execution — API calls, data transformations, file operations, database queries.

Each script should:
- Accept inputs via arguments or environment variables
- Return structured output (JSON preferred)
- Be independently testable
- Load credentials from `.env` only
